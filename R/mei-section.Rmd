---
output: html_document
editor_options: 
  chunk_output_type: console
---
# sdf

```{r eval=FALSE, include=FALSE}
world.regions <- establish.regions(levels(data.melt$country))
data.map(world.regions, "gini_avg", palette="RdYlBu", reverse.palette=T, domain.stretch=F)
```


```{r}
highcharter::hchart(data.melt.avg, "point",
          highcharter::hcaes(education_avg, gini_avg, group = continent),
          regression = TRUE, regressionSettings = list(hideInLegend = TRUE)) %>% 
  hc_colors(RColorBrewer::brewer.pal(8, "Set1")) %>%
  highcharter::hc_xAxis( max= data.melt.avg$education_avg %>% abs() %>% max(na.rm = T) %>% ceiling() ) %>%
  highcharter::hc_add_dependency("plugins/highcharts-regression.js")
```



```{r echo=FALSE}
data.melt.std.comb <- data.melt.standardized %>% 
  dplyr::select(continent, gini, education_discrepancy, babies_per_woman, suicide) %>%
  reshape2::melt(id="continent")
  
plot.violin(data.melt.std.comb, highcharter::highchart(), "value", "continent", "variable", highcharter::data_to_boxplot(data.melt.std.comb, value, continent, variable)) %>%
  hc.label("continent", "Standardized Value")
```
